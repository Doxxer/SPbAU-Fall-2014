package stopwatch.lang.structure;

/*Generated by MPS */

import jetbrains.mps.smodel.PropertySupport;
import java.util.Iterator;
import jetbrains.mps.internal.collections.runtime.ListSequence;

public class Form_PropertySupport extends PropertySupport {
  public boolean canSetValue(String value) {
    if (value == null) {
      return true;
    }
    Iterator<Form> constants = ListSequence.fromList(Form.getConstants()).iterator();
    while (constants.hasNext()) {
      Form constant = constants.next();
      if (value.equals(constant.getName())) {
        return true;
      }
    }
    return false;
  }

  public String toInternalValue(String value) {
    if (value == null) {
      return null;
    }
    Iterator<Form> constants = ListSequence.fromList(Form.getConstants()).iterator();
    while (constants.hasNext()) {
      Form constant = constants.next();
      if (value.equals(constant.getName())) {
        return constant.getValueAsString();
      }
    }
    return null;
  }

  public String fromInternalValue(String value) {
    Form constant = Form.parseValue(value);
    if (constant != null) {
      return constant.getName();
    }
    return "";
  }
}
